###################### APP ######################
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: $APP_NAME
  labels:
    name: $APP_NAME
spec:
  selector:
    matchLabels:
      name: $APP_NAME
  template:
    metadata:
      labels:
        name: $APP_NAME
    spec:
      containers:
        - name: $APP_NAME
          image: $APP_DOCKER_IMAGE_NAME
          imagePullPolicy: Never
          ports:
            - containerPort: $APP_PORT
          envFrom:
            - secretRef:
                name: $SECRET_NAME
            - configMapRef:
                name: $CONFIGMAP_NAME
          readinessProbe:
            httpGet:
              path: $APP_HEALTHCHECK_PATH
              port: $APP_PORT
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 2
          livenessProbe:
            httpGet:
              path: $APP_HEALTHCHECK_PATH
              port: $APP_PORT
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 2

---
apiVersion: v1
kind: Service
metadata:
  name: $APP_NAME-service
spec:
  selector:
    app.kubernetes.io/name: $APP_NAME
  ports:
    - protocol: TCP
      port: $APP_PORT
      targetPort: $APP_PORT

###################### CONFIG ######################
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: $CONFIGMAP_NAME
data:
  MAIN_DB_NAME: "fp_ms_dev"
  MAIN_DB_HOST: "10.101.196.171:${DB_SERVICE_TARGET_PORT}"
  MAIN_DB_PORT: "${DB_SERVICE_TARGET_PORT}"

---
apiVersion: v1
kind: Secret
metadata:
  name: $SECRET_NAME
type: Opaque
data:
  MAIN_DB_USERNAME: postgres
  MAIN_DB_PASSWORD: postgres

###################### DB ######################
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: $DB_SERVICE_NAME
  labels:
    name: $DB_SERVICE_NAME
spec:
  selector:
    matchLabels:
      app: $DB_SERVICE_NAME
  serviceName: $DB_SERVICE_NAME
  replicas: 1 # by default is 1
  minReadySeconds: 0 # by default is 0
  template:
    metadata:
      labels:
        app: $DB_SERVICE_NAME
    spec:
      containers:
        - name: $DB_SERVICE_NAME
          image: postgres:latest
          ports:
            - containerPort: $DB_SERVICE_TARGET_PORT
          volumeMounts:
          - name: $DB_SERVICE_VOLUME_NAME
            mountPath: /data
          env:
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: $SECRET_NAME
                  key: MAIN_DB_USERNAME
                  optional: false
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: $SECRET_NAME
                  key: MAIN_DB_PASSWORD
                  optional: false
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: $CONFIGMAP_NAME
                  key: MAIN_DB_NAME
                  optional: false
            - name: PGPORT
              valueFrom:
                configMapKeyRef:
                  name: $CONFIGMAP_NAME
                  key: MAIN_DB_PORT
                  optional: false
  volumeClaimTemplates:
  - metadata:
      name: $DB_SERVICE_VOLUME_NAME
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: $DB_SERVICE_NAME-service
spec:
  selector:
    app.kubernetes.io/name: $DB_SERVICE_NAME
  ports:
    - protocol: TCP
      port: $DB_SERVICE_TARGET_PORT
      targetPort: $DB_SERVICE_TARGET_PORT
